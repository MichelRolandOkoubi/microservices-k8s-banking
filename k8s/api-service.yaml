apiVersion: v1
kind: Service
metadata:
  name: banking-api-service
  namespace: banking-prod
spec:
  type: LoadBalancer
  selector:
    app: banking-api
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
